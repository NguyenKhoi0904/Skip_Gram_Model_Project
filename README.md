English:

GK is a folder storing exercises from the middle exam.

Skip-Gram\_Model is a folder storing file code for base Skip-Gram model and Skip-Gram model on each data type (Vietnamese and English data).

Vietnamese:

GK là thư mục lưu trữ bài tập của bài kiểm tra giữa kỳ.

Skip-Gram\_Model là thư mục lưu trữ file code về mô hình Skip-Gram cơ bản và mô hình Skip-Gram trên từng loại dữ liệu (dữ liệu tiếng Việt và tiếng Anh).

